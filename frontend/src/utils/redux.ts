import type {createAsyncThunk} from "@reduxjs/toolkit";

// TODO: create function which gonna help escape redux boilerplate (addCase, etc)
export const createAsyncAction = (
	_: ReturnType<typeof createAsyncThunk>,
) => {

}